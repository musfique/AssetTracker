@model IEnumerable<AssetTracker.Core.Models.GeneralCategory>

@section css{
    @Styles.Render("~/Content/datatablecss")
}


@{
    ViewBag.Title = "Index";
}
<div class="container">
    <div class="panel panel-primary">
        <div class="panel-heading">View All General Categories</div>
        <div class="panel-body">
            <div class="form-group">
                <div class="col-md-12">
                    <input type="button" value="Create" class="btn btn-success" onclick="location.href = '@Url.Action("Create", "GeneralCategory")'" />
                </div>
            </div>
            <table class="table table-hover table-background table-bordered">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.GeneralCategoryName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.GeneralCategoryCode)
                        </th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model) {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.GeneralCategoryName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.GeneralCategoryCode)
                            </td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", new { id = item.GeneralCategoryID }) |
                                @Html.ActionLink("Details", "Details", new { id = item.GeneralCategoryID }) |
                                @Html.ActionLink("Delete", "Delete", new { id = item.GeneralCategoryID })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts{
    @Scripts.Render("~/bundles/datatablejs")
    <script>
        $(document).ready(function () {
            $(".table").DataTable();
        });
    </script>
}
